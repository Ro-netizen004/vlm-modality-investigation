# Python
__pycache__/
*.py[cod]
*.pyd
*.so
.Python

# Virtual environments
.venv/
venv/
ENV/
env/

# Jupyter / IPython
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Cursor
.cursor/

# Secrets
.env

# Logs
*.log

# Generated experiment outputs
vlm_benchmark_outputs/
rendered_images/
presentations/

# HF v2 upload staging (large; publish to Hub, do not commit)
hf_gsm8k_v2_upload/

# Generated metadata/artifacts
gsm8k_metadata*.csv
render_config.json
render_progress.json
render_samples/
data/gsm8k_metadata*.csv
data/render_config.json
data/render_progress.json

# Paper drafts — keep local until deliberate arXiv/submission
paper/



# Generated conflict dataset (large; lives on HF, not git)
data/conflict_dataset*

# Large per-trial legibility data — kept on Drive/cluster, not git (see results/README.md).
# Keeps the small JSON summaries + .cll.jsonl margins tracked; excludes the heavy raw files.
# (phase1/phase3 CSVs are intentionally NOT ignored — policy keeps those committed.)
results/phase6_legibility/**/*.csv
results/phase7_text_legibility/**/*.csv
results/**/*.logprobs.jsonl
results/**/*.partial.jsonl
