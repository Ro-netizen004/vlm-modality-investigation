torch>=2.0.0
transformers==4.49.0  # pinned: >=4.50 drops GenerationMixin (breaks InternVL2/MiniCPM); Qwen2.5-VL needs >=4.49
accelerate
datasets
evaluate
scipy
numpy
pandas
matplotlib
Pillow
tqdm
pyyaml
bitsandbytes
pyarrow
huggingface_hub
torchvision
openai  # frontier API models (GPT-*) via VLMModel type="openai"
google-genai  # frontier API models (Gemini-*) via VLMModel type="gemini"
pytesseract  # OCR char-survival legibility axis (needs the `tesseract` binary); phase_control only
